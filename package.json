{
  "name": "streamer",
  "version": "1.0.1",
  "description": "A powerful video streaming library for multiple platforms using Bun, TypeScript, and FFmpeg",
  "module": "src/index.ts",
  "type": "module",
  "author": "mewisme",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/mewisme/streamer.git"
  },
  "scripts": {
    "build": "bun build src/index.ts --outdir dist --target bun --minify",
    "build:crawler": "bun build src/utils/tiktok.ts --outdir dist/utils --target bun --minify"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/cors": "^2.8.19",
    "@types/express": "^5.0.3"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "chalk": "^5.4.1",
    "cors": "^2.8.5",
    "express": "^5.1.0",
    "puppeteer-core": "^24.10.0"
  }
}